---
title: "Notes"
---

```bash
$ git status
On branch dev
nothing to commit, working tree clean
```

```yaml
project:
  type: website
  output-dir: _site
```

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

```
plain block, no language — should label as "text"
second line for the line numbers
third line
```
